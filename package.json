{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "nx": "^14.5.2"
  },
  "scripts": {
    "dev": "lerna run dev --scope @db-man/components",
    "test": "lerna run test",
    "build": "lerna run build",
    "prepublishOnly": "npm run build"
  }
}
